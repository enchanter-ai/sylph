{
  "name": "@enchanter-ai/agent-foundations-meta",
  "version": "0.1.0",
  "private": true,
  "description": "Meta-package for cross-repo versioning via changesets",
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@changesets/changelog-github": "^0.5.0"
  }
}
